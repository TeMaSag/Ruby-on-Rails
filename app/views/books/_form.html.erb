<%= form_with model: @book, local: true do |form| %>

  <% if @book.errors.any? %>
    <div id="error_explanation">
      <h2>
        <%= pluralize(@book.errors.count, "error") %> prohibited
        this book from being saved:
      </h2>
      <ul>
        <% @book.errors.full_messages.each do |msg| %>
          <li><%= msg %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <p>
    <%= form.label :Library %><br>
    <%= form.select ( :Library, Book.all.map {|c| [c.author, c.id]}) %>
  </p>

  <p>
    <%= form.label :TitleOfTheBook %><br>
    <%= form.text_field :TitleOfTheBook %>
  </p>

  <p>
    <%= form.label :author %><br>
    <%= form.text_area :author %>
  </p>

  <p>
    <%= form.label :cipher %><br>
    <%= form.text_area :cipher %>
  </p>

  <p>
    <%= form.label :publishing_house %><br>
    <%= form.text_area :publishing_house %>
  </p>

  <p>
    <%= form.label :price %><br>
    <%= form.text_area :price %>
  </p>

  <p>
    <%= form.label :receipt_date %><br>
    <%= form.text_area :receipt_date %>
  </p>

  <p>
    <%= form.submit %>
  </p>

<% end %>